# when passing a block into with(), if it fails it says it was not called
# when expecting at least once, the error is "exactly once"
# if you create a mock using just a symbol, it assumes it is a hash of expectations, not the name.
# idea - be able to halt method execution after last expectation has been reached. 

# other...
# git issue - can't easily call commands from outside dir
# js only video streaming
